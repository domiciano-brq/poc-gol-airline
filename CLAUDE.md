# CLAUDE.md

## Stack

- No stack detected from repository structure.

## Estrutura

docs/    # Documentacao do projeto
## Comandos

Nenhum `package.json` ou script de build detectado.

## Convencoes

- Documentacao centralizada em `docs/`
- `CLAUDE.md` na raiz define o contexto do projeto para agentes de IA

## Observacoes

Estrutura minima detectada (`docs/` e `CLAUDE.md`). Adicione arquivos de codigo-fonte, `package.json` e configuracoes para uma analise mais rica de convencoes e padroes.